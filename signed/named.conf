options {
    directory "/var/cache/bind";

    listen-on { 172.30.0.40; };
    listen-on-v6 { none; };

    recursion no;

    allow-query { any; };
};

zone "signed" {
    type primary;
    file "/var/lib/bind/db.signed.signed";
};
