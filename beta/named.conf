options {
    directory "/var/cache/bind";

    listen-on { 172.30.0.50; };
    listen-on-v6 { none; };

    recursion no;

    allow-query { any; };
};

zone "beta.signed" {
    type primary;
    file "/var/lib/bind/db.beta.signed.signed";
};


